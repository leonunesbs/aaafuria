import { ThemeOverride } from '@chakra-ui/react';

const override: ThemeOverride = {
  fonts: {
    heading: 'AACHENN, sans-serif',
    body: 'Lato, sans-serif',
  },
};

export default override.fonts;
